# Memory
maxmemory 128mb
maxmemory-policy allkeys-lru

# Persistence (AOF)
appendonly yes
appendfsync everysec

# Security
# Password is set via command line (--requirepass)

# Performance
tcp-keepalive 300
timeout 0
