schema = 1
id = "API-020"
type = "interface"
title = "Per-user traffic lookup index path"
description = "Indexes on devices by user and on traffic_logs by device with a descending timestamp, so the per-user per-node breakdown reaches one user's rows instead of scanning the window for every user."
status = "active"
created_at = 2026-09-19T22:38:18+09:00
updated_at = 2026-09-19T22:38:18+09:00

[metadata]
kind = "storage"

[[relations]]
to = "REQ-020"
type = "implements"
