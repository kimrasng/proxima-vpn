schema = 1
id = "RSK-001"
type = "risk"
title = "Adding query parameters to the shared activity endpoint could break the dashboard feed"
description = "The dashboard consumes the same endpoint. If the filter is applied when parameters are absent or malformed, the dashboard feed would silently empty."
status = "active"
created_at = 2026-09-19T00:39:40+09:00
updated_at = 2026-09-19T00:39:40+09:00

[metadata]
likelihood = "low"
mitigation = "Apply the filter only when both target parameters are non-empty, and cover the unfiltered path with a regression test"
severity = "medium"
